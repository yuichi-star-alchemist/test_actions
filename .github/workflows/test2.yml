name: Run echo_programs in GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  echo_programs:
    runs-on: ubuntu-latest
    steps:
      - name: Run echo programs1
        run echo '/usr/bin/ is :' && ls -al /usr/bin

      - name: Run echo programs2
        run echo '/usr/local/bin/ is :' && ls -al /usr/local/bin

      # - name: Set up Node.js
      #   uses: actions/setup-node@v3  #👈Node.jsを使えるようにする
      #   with:
      #     node-version: '20'

      # - name: Install dependencies
      #   run: npm install

      # - name: Run script
      #   run: node index.js