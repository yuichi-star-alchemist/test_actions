name: Frontend CI

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - ".github/**"

jobs:
  Lint_and_Test:
    runs-on: ubuntu-latest
    steps:
      - name: Use Repository
        uses: actions/checkout@v4
      - run: pwd
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.11.0"
          cache: "npm"
      
      - run: npm ci

      - name: Run Lint
        run: npm run lint
      
      - name: Run Test
        run: npm run test